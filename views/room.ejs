<!DOCTYPE html>
<html lang="en">
<head>
  <link type="image/png" href="https://p.kindpng.com/picc/s/75-757133_flat-video-icon-png-samsung-video-call-icon.png" rel="icon"> 
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <script>
    const ROOM_ID = "<%= roomId %>"
  </script>

 
  
  <title>Video Calling App</title>
  <style>
    /*#video-grid {
      display: grid;
      bottom: 100px;
      grid-template-columns: repeat(auto-fill, 100%);
      grid-auto-rows: 90%;
    }*/
    * {
      box-sizing: border-box;
    }
    
    /* Create four equal columns that floats next to each other */
    .column {
      float: left;
      width: 25%;
      height: 300px;
      padding: 10px;
    }
    
    /* Clear floats after the columns */
    .row:after {
      content: "";
      display: table;
      clear: both;
    }
    
    /* Responsive layout - makes a two column-layout instead of four columns */
    @media screen and (max-width: 900px) {
      .column  {
        width: 50%;
        height: 300px;
      }
    }
    
    /* Responsive layout - makes the two columns stack on top of each other instead of next to each other */
    @media screen and (max-width: 600px) {
      .column  {
        width: 100%;
      }
    }
    video {
      width: 100%;
      height: 300px;
      object-fit: cover;
    }
    .label{
      position:absolute;
      font-size: large;
      color: #ddd;
      font-family: 'Times New Roman', Times, serif;
    }
    #bottom-nav{
      position: fixed;
      bottom: 0;
      text-align: center;
      margin: auto;
      padding: 10px 10px;
      left: 0;
      display: inline-block;
      background-color: aquamarine;
      right: 0;
      height: 60px;
      width: 100%;
    }
    button{
      border: none;
      margin: auto;
      align-items: center;
      background-color: #fff;
    }
    img{
      width: 60px;
      height: 60px;
    }
  </style>
  <script src="/socket.io/socket.io.js" ></script>
</head>
<body>
  <div id="video-grid" class="row">
    
      <div class="column">
        <label class="label" id="label"></label>
      <video id="localVideo" muted autoplay></video>
      </div>
  
  </div>
  <div id="bottom-nav">
    <button ><img src="micon" id="micon" ></button>
    <button id="micoff" style="display: none;" ><img src="micoff"></button>
    <button  ><img src="videoon" id="videoon" ></button>
    <button id="videooff" style="display: none;"><img src="videooff"></button>
  </div>
  <script src="script.js"></script>
  <script type="text/javascript">
    var parent = document.getElementById("video-grid");
    var childs = parent.children;
    if(childs.length >4){
     // parent.style.grid-template-columns = "repeat(auto-fill, 50%)";
    }
    
  </script>
</body>
</html>